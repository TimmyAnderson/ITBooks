//-----------------------------------------------------------------------------
#pragma once
//-----------------------------------------------------------------------------
#include <chrono>
#include <thread>
#include "MyDebug.h"
//-----------------------------------------------------------------------------
//-----------------------------------------------------------------------------
//-----------------------------------------------------------------------------
class CThreadMethod
{
//-----------------------------------------------------------------------------
	private:
		int														MIndex;

	public:
		void ThreadMethod(int Index)
		{
			std::wcout << L"THREAD METHOD [" << MIndex << L"," << Index << L"] STARTED !" << std::endl;

			// Caka sa 5 sekund.
			std::this_thread::sleep_for(std::chrono::duration<int,std::ratio<1,1>>(5));

			std::wcout << L"THREAD METHOD [" << MIndex << L"," << Index << L"] FINISHED !" << std::endl;
		}

	public:
		CThreadMethod(int Index)
			: MIndex(Index)
		{
		}
//-----------------------------------------------------------------------------
};
//-----------------------------------------------------------------------------