//-------------------------------------------------------------------------------------------------------
1. Solution demonstruje INSTANCE MODE Singleton.
2. SINGLETON rezim sa urci definovanim [ServiceBehavior(InstanceContextMode=InstanceContextMode.Single)] na TRIEDU SERVICE.
3. SINGLETON vznikne ked je zavedeny HOST a konci ked skonci HOST.
!!! 4. Je mozne vytvorit vlastnu instanciu SINGLETON a tu podhodit HOST.
//-------------------------------------------------------------------------------------------------------